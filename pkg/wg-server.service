[Unit]
Description=WireGuard Server Monitoring Daemon
After=network.target
Before=trim_http_cgi.service

[Service]
Type=simple
ExecStart={{BINARY}} daemon
Environment=TRIM_PKGVAR={{PKG_VAR}}
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
